%nav.navbar.navbar-default.navbar-fixed-top{role: "navigation"}
  .container-fluid
    .navbar-header
      %button.navbar-toggle{"data-target" => "#hiresly-navbar-collapse", "data-toggle" => "collapse", type: "button"}
        %span.sr-only Toggle navigation
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      %a.navbar-brand{href: "/"} Hiresly
    .collapse.navbar-collapse#hiresly-navbar-collapse
      %ul.nav.navbar-nav.pull-right
        %p.navbar-text.pull-right
        - if user_signed_in?
          %li
            =link_to edit_user_registration_path, class: ['navbar-link'] do
              Edit profile
          %li
            =link_to destroy_user_session_path, method: :delete, class: ['navbar-link'] do
              Logout
        - else
          %li
            =link_to new_user_registration_path, class: ['navbar-link'] do
              Sign up
          %li
            =link_to new_user_session_path, class: ['navbar-link'] do
              Login
